# Base docker image

## Available builds

- `rokasm/base:1.0-7.4`
- `rokasm/base:1.0-7.3`

## Exec

- ``docker exec -e COLUMNS="`tput cols`" -e LINES="`tput lines`" -u laradock -it workspace /bin/zsh``
- sudo password: `laradock`
